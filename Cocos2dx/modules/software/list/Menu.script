function setData(data) {
    _super.prototype.setData.call(this, data);
    var menu = data.menu || [];
    var d = new flower.ArrayValue();
    for (var i = 0; i < menu.length; i++) {
        d.push(menu[i]);
    }
    this.list.dataProvider = d;
}

function clickItem(e) {
    if (this.data.click) {
        this.data.click(this.data.target, e.item);
    }
}